# Neural Visualization
This Python program simulates and visualizes a simple feedforward neural network as it learns from random input-output pairs. It shows how the network’s weights change in real time during training. The network is defined by a list of layer sizes, e.g.: [9, 7, 5, 3]  # 9 inputs → 2 hidden layers → 3 outputs
